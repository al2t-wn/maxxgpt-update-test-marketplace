---
name: update-probe
description: >-
  ติดตั้งหรืออัปเดตหน้าทดสอบ "MaxxGPT Update Probe" ในบัญชีนี้ เพื่อพิสูจน์ว่าปลั๊กอินเวอร์ชันใหม่
  ส่งถึงบัญชีลูกค้าและอัปเดตหน้าเดิมที่ลิงก์เดิมได้จริง. ใช้เมื่อผู้ใช้พิมพ์ /update-probe หรือพูดว่า
  "ทดสอบอัปเดต", "ติดตั้งหน้าทดสอบอัปเดต", "อัปเดตหน้า probe", "run the update probe".
  ถ้าผู้ใช้ให้ลิงก์ release มาด้วย ให้ใช้สกิล update-probe-from-url แทน.
---

# Update Probe · แม่แบบเวอร์ชัน 2 · ปลั๊กอิน 0.2.0

สกิลนี้เป็นเครื่องมือทดสอบ ไม่ใช่ฟีเจอร์สำหรับลูกค้า เป้าหมายคือได้ผลจริงของช่องทางอัปเดต
**ทำตามขั้นตอนตรง ๆ ห้ามข้ามขั้น และห้ามหาทางอ้อมเมื่อเจอปัญหา** ให้หยุดแล้วรายงานข้อผิดพลาดตามจริง
เพราะทางอ้อมจะทำให้ผลทดสอบไม่ตรงกับสิ่งที่ลูกค้าจะเจอ

## ค่าของเวอร์ชันนี้

| ค่า | ข้อมูล |
|---|---|
| เวอร์ชันแม่แบบ | 2 |
| เวอร์ชันปลั๊กอิน | 0.2.0 |
| ไฟล์แม่แบบ | `assets/probe.html` ในโฟลเดอร์ของสกิลนี้ |
| sha256 ที่ถูกต้อง | `6cad9617360507c0645af7c0a9602b18c2d0213c47c76af5aae3e0453ca6c67e` |
| ชื่อหน้า | `MaxxGPT Update Probe` |

## ขั้น 1 · ตรวจไฟล์แม่แบบ

ไฟล์อยู่ที่ `<Base directory ของสกิลนี้>/assets/probe.html` โดยใช้ Base directory ที่ระบบแจ้งตอนโหลดสกิล

คำนวณ sha256 ด้วยคำสั่งที่เครื่องมี เช่น `sha256sum`, `shasum -a 256` หรือ PowerShell `Get-FileHash -Algorithm SHA256`

- ตรงกับค่าในตาราง = ผ่าน
- ไม่ตรง = **หยุด** รายงานว่า "ไฟล์แม่แบบไม่ตรง" พร้อมค่าที่คำนวณได้ ห้าม publish

**ห้ามแก้ไฟล์นี้ และห้ามเขียนไฟล์นี้ขึ้นใหม่** publish จากไฟล์เดิมเท่านั้น

## ขั้น 2 · connector และ capabilities

เวอร์ชันนี้ขอใช้ connector 1 ตัว คือ MaxxGPT ทำขั้นตอนนี้กับทีละ connector

1. เรียกสกิล `artifact-capabilities` ซึ่งจะแสดงรายชื่อ claude.ai connector ของเซสชันนี้ คู่กับ id ที่ใช้ในชื่อ tool
2. หา connector จาก tool ที่มันมี ไม่ใช่จากชื่อ โดยดูชื่อ tool ในเซสชันที่เป็นแบบ `mcp__<id>__<ชื่อ tool>`
   แล้วจับคู่ id กับชื่อที่แสดง ใช้ได้เฉพาะ claude.ai connector เท่านั้น MCP server ที่มาจากปลั๊กอินใช้ไม่ได้

| connector | tool ที่ใช้หา |
|---|---|
| MaxxGPT | `get_account_info` |

3. เจอตัวเดียว = ใช้ชื่อนั้น · เจอหลายตัว = ถามผู้ใช้ · ไม่เจอ = บอกให้เพิ่ม connector ที่ claude.ai
   ไปที่ Settings และ Connectors แล้ว**หยุด** และรายงานว่าไม่พบ connector ตัวไหน
   URL ของ MaxxGPT คือ `https://mcp.maxxgpt.ai/mcp` · URL ของ Meta ads คือ `https://mcp.facebook.com/ads`
4. ลองเรียกของจริงหนึ่งครั้งต่อ connector ถ้าได้ error ให้หยุด แล้วคัดลอกข้อความ error ลงรายงาน
   - เรียก `get_account_info` หนึ่งครั้ง tool นี้อ่านอย่างเดียวและไม่ต้องใส่อะไร

capabilities ที่ต้องส่งตอน publish คือ แทน `<ชื่อ MaxxGPT ที่พบ>` ด้วยชื่อที่พบในข้อ 3

```
{"db": {}, "mcp": {"servers": [{"server": "<ชื่อ MaxxGPT ที่พบ>", "tools": ["get_account_info"]}]}}
```

ประกาศ connector ละ**ชื่อเดียว** เท่านั้น ห้ามใส่ชื่อสำรอง เพราะชื่อที่บัญชีไม่มีจะทำให้หน้าขออนุญาตเด้งถามทุกครั้งที่เปิด

## ขั้น 3 · หาหน้าเดิม

1. เรียก `Artifact` ด้วย `action: "list"` และ `limit: 50`
2. หาหน้าที่ชื่อตรงกับ `MaxxGPT Update Probe` ทุกตัวอักษร
   เจอหลายหน้า ให้ใช้หน้าที่แก้ล่าสุด แล้วบอกในรายงานว่าเจอกี่หน้า
3. เจอหน้าเดิม ให้เรียก `Artifact` ด้วย `action: "read"` ที่ลิงก์นั้นก่อนเสมอ
   แล้วหาข้อความ `maxxgpt-update-probe version=` ใน HTML ตัวเลขที่ตามมาคือเวอร์ชันก่อนอัปเดต

## ขั้น 4 · publish

**เจอหน้าเดิม** ให้ publish ทับลิงก์เดิม

- `file_path` = ไฟล์แม่แบบจากขั้น 1
- `url` = ลิงก์ของหน้าเดิม
- `capabilities` = ค่าจากขั้น 2
- ห้ามส่ง `icon`, `contract` และ `title`

ถ้าถูกปฏิเสธเพราะยังไม่ได้อ่านหน้าเดิม หรือเพราะมีเวอร์ชันใหม่กว่าชนกัน ให้ publish ซ้ำพร้อม `force: true`
และจดในรายงานว่า "ต้องใช้ force" งานนี้ใช้ force ได้อย่างปลอดภัย เพราะข้อมูลทดสอบทั้งหมดอยู่ในฐานข้อมูลของหน้า
และหน้านี้ไม่เคย publish ตัวเอง ถ้าถูกปฏิเสธด้วยเหตุผลอื่น ให้คัดลอกข้อความ error ลงรายงานแล้วหยุด

**ไม่เจอหน้าเดิม** ให้ publish เป็นหน้าใหม่

- ไม่ส่ง `url`
- `icon` = `"test"`
- `description` = `"หน้าทดสอบว่าเวอร์ชันใหม่ของปลั๊กอินส่งถึงบัญชีนี้ได้จริง"`
- `capabilities` = ค่าจากขั้น 2

🚨 **ห้ามสร้างหน้าใหม่เมื่อมีหน้าเดิมอยู่แล้ว** คำถามหลักของการทดสอบคือลิงก์เดิมได้เวอร์ชันใหม่หรือไม่

## ขั้น 5 · ตรวจหลัง publish

เรียก `Artifact` ด้วย `action: "read"` ที่ลิงก์เดิมอีกครั้ง แล้วตรวจสองข้อ

- ข้อความ `maxxgpt-update-probe version=` ต้องตามด้วย 2 และมี `variant=normal`
  ถ้าก่อนหน้านี้หน้าเป็น `variant=drive` การ publish รอบนี้คือการถอด Google Drive ออก ให้จดไว้ในรายงานด้วย
- ถ้าเจอหน้าเดิมในขั้น 3 ลิงก์ต้องเป็นลิงก์เดียวกัน

## ขั้น 6 · รายงาน

ส่งแบบฟอร์มนี้ให้ผู้ใช้ในกล่องโค้ด โดยเติมทุกบรรทัด

```
ผลทดสอบ update-probe
วันเวลา:
แอปที่ใช้: Claude desktop โหมด Cowork / Claude Code / อื่น ๆ
เวอร์ชันปลั๊กอินที่สกิลนี้มาจาก: 0.2.0
checksum แม่แบบ: ตรง / ไม่ตรง
connector MaxxGPT: ชื่อที่พบ / เวอร์ชันนี้ไม่ใช้ / ไม่พบ
หน้าเดิม: เจอ เวอร์ชัน N / ไม่เจอ
การ publish: ทับลิงก์เดิม / ทับลิงก์เดิมด้วย force / สร้างหน้าใหม่ / ล้มเหลว
ลิงก์:
เวอร์ชันหลัง publish:
ข้อผิดพลาดที่เจอ:
```

จากนั้นบอกผู้ใช้ให้เปิดลิงก์แล้วทำตามรายการ "สิ่งที่ต้องตรวจในรอบนี้" บนหน้า
และขอให้เขาเล่ากลับมาว่าเห็นอะไร **ห้ามสรุปว่าผ่าน จนกว่าผู้ใช้ยืนยันว่าเปิดหน้าแล้วเห็นตามรายการ**
